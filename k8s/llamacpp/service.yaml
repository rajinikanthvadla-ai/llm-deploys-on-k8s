apiVersion: v1
kind: Service
metadata:
  name: llamacpp
  namespace: llm
  labels:
    app: llamacpp
spec:
  type: NodePort
  selector:
    app: llamacpp
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30080
